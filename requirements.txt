# Core chess logic
python-chess>=1.999

# Optional: ONNX runtime for neural-net evaluation in the mentor bot
onnxruntime>=1.20

# ── Legacy Pygame UI (kept for reference; not used by Electron app) ──────────
# pygame>=2.6
# fpdf2>=2.8

# Development / testing
pyinstaller>=6.0   ; extra == "build"
